
numeros = [12, 7, 5, 18, 22, 9, 3, 40]

pares = 0
impares = 0

for num in numeros:
    if num % 2 == 0:
        pares += 1
    else:
        impares += 1

print(f"Lista: {numeros}")
print(f"Cantidad de pares: {pares}")
print(f"Cantidad de impares: {impares}")
