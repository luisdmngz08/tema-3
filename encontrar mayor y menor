
numeros = [45, 12, 89, 3, 67, 24]

mayor = numeros[0]
menor = numeros[0]

for num in numeros:
    if num > mayor:
        mayor = num
    if num < menor:
        menor = num

print(f"Lista: {numeros}")
print(f"El número mayor es: {mayor}")
print(f"El número menor es: {menor}")
